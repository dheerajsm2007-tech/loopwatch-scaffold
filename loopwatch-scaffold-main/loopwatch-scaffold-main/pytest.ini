[pytest]
# Scope collection to this project's own tests. Without this, pytest's
# default recursive discovery also picks up fixtures/workspaces/basic_repo's
# synthetic tests/ (eval-corpus seed data, not real project tests) and fails
# to collect them since that fixture repo isn't on sys.path.
testpaths = tests
